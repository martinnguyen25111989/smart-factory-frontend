---
description: Bootstrap the project for a fresh checkout
---

Set up the project so a new contributor can run it locally.

1. Verify Node version against `package.json` `engines` field
2. `npm install`
3. Copy any `.env.example` → `.env` if present
4. Print the dev-server command and default URL (`http://localhost:4200`)
